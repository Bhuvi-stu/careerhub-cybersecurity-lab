This public-safe package was built from the files supplied in the chat. HTML templates/static assets were not included in the uploaded file set; copy your existing templates/ and static/ folders into this project before running it.